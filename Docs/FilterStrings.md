<!-- TODO: document dsl syntax for filtering LogStreams. See Remove-MailStream for useage example. Write private function for parsing filters. Split on ; first, trim whitespace, tokenize list values (comma separated), Where-Object on AugerContext to filter --> 
